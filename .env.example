BPM_BASE_URL="https://efgp.yuchens.com:8086/NaNaWeb/GP//ForwardIndex?hdnMethod=findIndexForward"
PLAYWRIGHT_BPM_USER=boyawu
PLAYWRIGHT_BPM_PASSWORD=

# Optional: zh or en
BPM_LOGIN_LOCALE=zh

# Optional safety guard for pagination loop
BPM_MAX_PAGES=100

# Optional target project code for workitemCheck spec
BPM_TARGET_PROJECT_CODE=DY23-0742

# Optional approval comment text (for summary/config visibility)
BPM_APPROVAL_COMMENT=請補上 WorkItem。

# Optional: set true to actually click "退回重辦"
BPM_ENABLE_REEXECUTE=false
